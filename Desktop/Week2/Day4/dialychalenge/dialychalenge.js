// const userInput = prompt("Enter several words separated by commas:");
// const words = userInput.split(',');
// let maxLength = 0;

// for (const word of words) {
//     const trimmedWord = word.trim();
//     if (trimmedWord.length > maxLength) {
//         maxLength = trimmedWord.length;
//     }
// }

// const border = '*'.repeat(maxLength + 4);

// console.log(border);
// words.forEach(word => {
//     const trimmedWord = word.trim();
//     const spaces = ' '.repeat(maxLength - trimmedWord.length);
//     console.log(`* ${trimmedWord}${spaces} *`);
// });
// console.log(border);